# `@sebastienrousseau/go-config` Architecture

Shareable Go configuration providing standardized golangci-lint, staticcheck, and revive rules adhering to modern 2026 standards.

## Design Principles
- Single source of truth for Go fleet configurations.
- Multi-format exports (CommonJS, ESM, TypeScript declarations).
- 100% test coverage with zero runtime production dependencies.
